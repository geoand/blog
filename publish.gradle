buildscript {
  repositories {
      jcenter()
  }

  dependencies {
    classpath 'org.ajoberstar:gradle-git:0.9.0'     
  }
}

apply plugin: 'github-pages'                        

githubPages {
  repoUri = 'git@github.com:geoand/blog.git'      
  pages {
     from(file('build/jbake')) {
     	into '.'                                    
     }
  }
}